namespace BAAS;

public partial class TestLayout : ContentPage
{
	//public TestLayoutOld()
	//{
	//	InitializeComponent();

	//	// Will be overriden in the xaml file if padding is specified there instead. 
 //       VerticalStackLayout.Padding =
 //           DeviceInfo.Platform == DevicePlatform.iOS ? new Thickness(20, 10) 
	//		: DeviceInfo.Platform == DevicePlatform.Android ? new Thickness(10, 5)
	//		: new Thickness(0, 10);

	//}
}